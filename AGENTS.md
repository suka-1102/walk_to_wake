<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# Walk to Wake

自分で設定した目標地点に、決めた時刻までに実際に足を運ぶ（＝チェックインする）ことを、デポジット（預り金）の減額というペナルティで後押しする Web アプリ。

アラーム機能は持たない。起こすのは既存のアラームアプリの役割で、本アプリが担うのは「鳴った後、二度寝せずに外へ出る」動機付けの部分。目的地は自分で設定する。

**v1 では実際の金銭は動かない。** Stripe による決済・返金は v2 以降のスコープで、v1 はペナルティ額の計算と表示までを行う。

現在は要件定義段階で、`app/` は create-next-app の初期状態。仕様は下表ですべて確定済み（未確定事項なし）。

このファイルが仕様・規約の正。以下は**作業の種類に応じて**読む（全部読む必要はない）。

| 読むタイミング | ファイル |
|---|---|
| スタイルを書くとき | [docs/style.md](docs/style.md) |
| テストを書くとき | [docs/testing.md](docs/testing.md) |
| コミット・PR を出すとき | [docs/workflow.md](docs/workflow.md) |
| 環境変数を追加・参照するとき | [docs/env.md](docs/env.md) |

## 使い方の流れ

1. 現地へ行き、ブラウザの Geolocation API で取得した座標をそのまま目標地点として登録する（地図上でピンを指定する方式ではない）
2. チェックイン期限時刻・チャレンジ期間（開始日〜終了日）・デポジット額を設定する
3. 期間中、毎日期限時刻までに目標地点付近でチェックインする
4. 間に合わなかった日／目標地点に到達しなかった日は失敗としてカウントされ、デポジット残高が減る
5. 終了日を迎えるか、残高がペナルティ単価を下回った時点でチャレンジ終了

## 仕様（決定事項）

| 項目 | 値・方針 |
|---|---|
| 判定半径 | 目標地点から 100m 以内。ただし、下記の精度ガードで締める |
| 位置精度ガード | Geolocation の `accuracy` が 50m を超える座標はチェックインにも目標地点登録にも使わない。|
| 精度不足時の挙動 | チェックインを成立させず、「精度が不十分。空の見える場所で再取得を」と表示して手動の再取得ボタンを出す。|
| チェックイン受付時間 | 当日 4:00（JST）〜期限時刻。それ以外は無効。期限に猶予はなく1秒でも過ぎたら失敗 |
| ペナルティ単価 | 1失敗あたり 500円の固定値|
| デポジット額 | 3,000円以上 30,000円以下。下限はペナルティ数回分の余地を残して早期失敗が即起きないように、上限は v2 で実決済になったときの預り金・返金リスクを抑えるために置く |
| 早期失敗 | 残高（デポジット額 − 失敗回数 × 500円）が 500円未満になった時点で即時終了。端数（0〜499円）は没収し返金しない。終了後はすぐ新しいチャレンジを作成できる |
| 目標地点の変更 | 1アカウントにつき累積 3回まで。直前に目標を自宅等へ変えて失敗を逃れるチートを防ぐため |
| 期限時刻の変更 | チャレンジ開始後は不可。後ろにずらして失敗を逃れる抜け道を防ぐため |
| 失敗回数の算出 | DB に保存するのは成功したチェックインだけ。失敗回数は画面を表示するたびに「期限を過ぎた日数 − チェックイン成功日数」で計算する。失敗を DB に持つと、誰も操作しなくても失敗を記録する仕組み（毎晩決まった時刻に自動実行される処理）が必要になるため |
| システム利用料 | デポジット額の 3%。返金対象外の運営収益 |
| タイムゾーン | サーバー・DB とも JST 固定。変換ロジックは持たない |
| 同時チャレンジ数 | 1ユーザー1件 |
| 途中離脱・中断 | 中断・一時停止・中途解約はいずれも不可。やめたい場合は放置するしかなく、失敗が積み上がって早期失敗で自然終了する。逃げ道を作らないという本アプリの前提と一貫させるため。「チャレンジを中止」ボタンは作らない |
| 位置情報の偽装 | v1 では既知のリスクとして許容し、検知・ブロックのコードは書かない。実金銭が動かないため偽装しても損得が発生しない。v2 で実決済を入れる際に再検討する |


**上表の数値は実装時に `lib/config.ts` へ移し、そちらを正とする。** 移行後この表には数値ではなく「なぜその値か」だけを残す。

## v1 のスコープ外

- Stripe 等による実決済・返金（方針は下記のとおり先に決めてある）
- グループ機能・他ユーザーとの共有
- プッシュ通知・アラーム音などのリマインド機能
- 複数チャレンジの同時進行

### 将来の Stripe 連携方針（決定済み・実装は v2 以降）

- **決済時**: 「デポジット額 ＋ システム利用料」を合算して決済する
- **返金時**: 期間満了まで継続した場合は「デポジット額 −（失敗回数 × ペナルティ単価）」を返金する。早期失敗の場合は 0円。システム利用料は返金しない
- 日本の Stripe 決済手数料は概ね 3.6% 前後のため、3% の利用料では完全には相殺できない。差分は許容する

#### v2 着手の前提条件（法務確認）

法務の問題がある可能性があるのでV2実装前に一度決済周りの法律を調べる。

## ドメイン用語

コードの識別子はこの語彙に揃える。

- **チャレンジ (challenge)**: 目標地点＋期限時刻＋期間＋金額のセット
- **目標地点 (target location)**: 緯度・経度。変更回数はアカウント単位の累積でチャレンジごとにリセットしない（`location_change_count` はユーザー側に持たせる）
- **チェックイン (check-in)**: 距離判定と時刻判定を両方クリアする行為。1日1回
- **失敗 (failure)**: チェックインできなかった日
- **デポジット (deposit)**: ユーザーが入力する金額（3,000〜30,000円）
- **位置精度 (accuracy)**: Geolocation が返す誤差半径。閾値を超える座標は採用しない
- **ペナルティ単価**: 1失敗あたりの減額
- **早期失敗**: 残高がペナルティ単価を下回り、チャレンジが即時終了した状態
- **システム利用料**: デポジット額に対する一定率の運営収益

## 技術スタック

- **Bun** — `npm` / `npx` / `yarn` は使わない（`npx` の代わりに `bunx`）
- **Next.js 16 (App Router) + React 19 + TypeScript** — `src/` は使わず `app/` はリポジトリ直下
- **CSS Modules + SCSS** — CSS-in-JS やユーティリティクラス方式は使わない
- **MySQL + Prisma** — スキーマ・マイグレーション・クエリはすべて Prisma 経由。生 SQL は書かない
- **Auth.js (next-auth)** — ソーシャルログイン。自前のパスワード管理はしない
- **Geolocation API** — 位置情報はブラウザから取得する（IP ジオロケーションは使わない）。目標地点の登録も現地で取得した座標をそのまま記録し、地図でピンを指定する UI は導入しない
- **Vitest** — 単体テスト
- **（v2 以降）Stripe** — v1 では API を一切呼ばない

未導入のものは使う段になってから `bun add` する。先回りして依存を追加しない。現状は Tailwind CSS v4 と `package-lock.json` が残っており、実装着手時に Tailwind 撤去・SCSS 移行・`bun.lock` 化を行う。

## コマンド

```bash
bun install                                # 依存インストール
bun run dev                                # 開発サーバー (http://localhost:3000)
bun run build                              # 本番ビルド。型エラーもここで出る
bun run lint                               # ESLint
bun add <パッケージ名>                      # 依存追加（開発用は -d）
bunx vitest                                # テスト
bunx prisma migrate dev --name <変更内容>   # マイグレーション作成・適用
bunx prisma generate                       # Prisma Client の型を再生成
bunx prisma studio                         # DB を GUI で確認
```

**変更後は必ず `bun run lint` と `bun run build` を通す。**

DB（MySQL / Prisma）と認証（Auth.js）のセットアップ手順は、実装に着手した時点でここに追記する。

## ディレクトリ構成（実装時にこの形に育てる）

- `app/` — ルーティング・画面・Route Handler
- `components/` — 再利用する UI コンポーネント
- `lib/` — ドメインロジック。距離判定・期限判定・金額計算は DB やリクエストに依存しない純粋関数として必ずここに置く
- `lib/config.ts` — しきい値・定数の集約先
- `prisma/schema.prisma` — DB スキーマ
- `styles/` — SCSS 変数（[docs/style.md](docs/style.md)）
- `docs/` — 開発ドキュメント

## コーディングルール

- **しきい値をハードコードしない。** 判定半径・位置精度の上限・デポジット額の上下限・変更回数の上限・システム利用料率などの数値はすべて `lib/config.ts` に集約する
- **金額計算・距離計算をコンポーネントに書かない。** `lib/` の純粋関数として実装し、コンポーネントからは呼ぶだけにする
- **Server Components をデフォルトにする。** `"use client"` はブラウザ API が必要な末端のコンポーネントに限定する
- **Stripe の決済 API を呼ぶコードを書かない。** v1 のスコープ外。金額計算までに留める
- 金額は整数（円）で扱う。浮動小数点で金額計算をしない
- 位置情報は個人情報。必要以上に保存せず、ログに座標を出力しない
- TypeScript は `strict: true`。`any` は使わず、必要なら `unknown` を経由する
- import は `@/` エイリアスを使う（`../../` を連ねない）
- UI 文言・コメント・コミットメッセージ・PR は日本語。識別子は英語

## 触ってはいけないファイル

- `bun.lock` — 手編集せず `bun` コマンド経由で更新する（コミット対象。削除しない）
- `prisma/migrations/` の**適用済み**ファイル — 書き換えず、新しいマイグレーションを追加して直す
- `node_modules/`、`.next/`、`next-env.d.ts` — 自動生成物
- 冒頭の `nextjs-agent-rules` ブロック — `next dev` が再生成する
- `.env*` — 秘密情報。[docs/env.md](docs/env.md) を参照
