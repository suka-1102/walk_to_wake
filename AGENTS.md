# Walk to Wake

自分で設定した目標地点に、決めた時刻までに実際に足を運ぶ（＝チェックインする）ことを、デポジット（預り金）の減額というペナルティで後押しする Web アプリ。

アラーム機能は持たない。起こすのは既存のアラームアプリの役割で、本アプリが担うのは「鳴った後、二度寝せずに外へ出る」動機付けの部分。目的地は自分で設定する。

**想定ユーザーは作者本人（大学生）。** 午前中から勉強に集中するために早起きしたいが、意志とアラームだけでは二度寝してしまい、お金が減らないと結局起きない。機能を足すか迷ったら「この1人に効くか」で判断し、万人向けの汎用化はしない。

**v1 では実際の金銭は動かない。** Stripe による決済・返金は v2 以降のスコープで、v1 はペナルティ額の計算と表示までを行う。

現在は要件定義段階で、`app/` は create-next-app の初期状態。仕様は下表ですべて確定済み（未確定事項なし）。実装の順番と進捗は [docs/roadmap.md](docs/roadmap.md) が正で、**コミットのたびにそちらのチェックリストを更新する**。

このファイルが仕様・規約の正。以下は**作業の種類に応じて**読む（全部読む必要はない）。

| 読むタイミング | ファイル |
|---|---|
| **実装を進めるとき（最初に読む）** | [docs/roadmap.md](docs/roadmap.md) |
| 画面を作るとき | [docs/screens.md](docs/screens.md) |
| スタイルを書くとき | [docs/style.md](docs/style.md) |
| テストを書くとき | [docs/testing.md](docs/testing.md) |
| コミット・PR を出すとき | [docs/workflow.md](docs/workflow.md) |
| 環境変数を追加・参照するとき | [docs/env.md](docs/env.md) |

## このファイルは AI が自動で修正・追記する

身に覚えのない差分が出たらまずこの節を疑うこと。書き換えの経路は 2 つある。

- **仕様・規約が変わったとき、AI エージェントがこのファイルを更新する。** 実装と仕様がズレたまま進めないよう、変更と同じコミットに含める。人が手で書き写す運用にはしない
- **`bun run dev`（`next dev`）が末尾に英文ブロックを自動で書き戻す。** AI エージェント環境を検知すると `<!-- BEGIN:nextjs-agent-rules -->` 〜 `<!-- END:nextjs-agent-rules -->` を追記する（`node_modules/next/dist/server/lib/generate-agent-files.js`。無効化する設定は用意されていない）。**このブロックは不要。** 差分に出てきたらブロックごと削除し、コミットには含めない。AI も人も手動で書き足さない

## 使い方の流れ

1. 目標地点を決める。初めての場所は現地へ行き、ブラウザの Geolocation API で取得した座標をそのまま登録する（地図上でピンを指定する方式ではない）。登録した地点はアカウントに保存され、次のチャレンジからは一覧で選べる
2. チェックイン期限時刻・チャレンジ期間（開始日〜終了日）・デポジット額を設定する
3. 期間中、毎日期限時刻までに目標地点付近でチェックインする
4. 間に合わなかった日／目標地点に到達しなかった日は失敗としてカウントされ、デポジット残高が減る
5. 終了日を迎えるか、残高がペナルティ単価を下回った時点でチャレンジ終了

## 仕様（決定事項）

| 項目 | 値・方針 |
|---|---|
| 判定半径 | `CHECK_IN_RADIUS_METERS`。実際にその場へ足を運んだと言える範囲に留める。広げすぎると家から出ずに通ってしまう。下記の精度ガードと合わせて締める |
| 位置精度ガード | `MAX_ACCURACY_METERS`。誤差の大きい座標を採用すると離れた場所からでもチェックインが通るため、判定半径より十分小さい値で弾く。チェックインにも目標地点登録にも同じ基準を使う |
| 精度不足時の挙動 | チェックインを成立させず、「精度が不十分。空の見える場所で再取得を」と表示して手動の再取得ボタンを出す。|
| チェックイン受付時間 | 当日の `CHECK_IN_START_HOUR` 〜期限時刻。それ以外は無効。前夜のうちに済ませられないよう開始時刻で切る。期限に猶予はなく1秒でも過ぎたら失敗 |
| ペナルティ単価 | `PENALTY_YEN`。1失敗あたりの固定値。惜しいと感じる額にする |
| デポジット額 | `MIN_DEPOSIT_YEN` 〜 `MAX_DEPOSIT_YEN`。下限はペナルティ数回分の余地を残して早期失敗が即起きないように、上限は v2 で実決済になったときの預り金・返金リスクを抑えるために置く |
| 早期失敗 | 残高（デポジット額 − 失敗回数 × ペナルティ単価）がペナルティ単価を下回った時点で即時終了。ペナルティ単価に満たない端数は没収し返金しない。終了後はすぐ新しいチャレンジを作成できる。**終了した後の日は失敗として数えない**（失敗回数は動的算出で日数が増え続けるため、デポジット額が吸収できるペナルティ回数を上限とする） |
| 目標地点の再利用 | 登録した目標地点はアカウントに保存し、次のチャレンジ作成時に一覧から選べる。毎回同じ場所まで足を運んで登録し直すのは手間なだけで、抑止力にはならないため。**目標地点になれるのは過去に一度は現地で取得した座標だけ**という不変条件は再利用でも崩れない |
| 目標地点の名前 | 保存時にユーザーが入力する。地図も逆ジオコーディングも持たないため、座標だけの一覧では選びようがない |
| チャレンジ開始後の変更 | 目標地点も期限時刻も変更不可。どちらも「後から条件を緩めて失敗を逃れる」同じ形のチートなので、回数制限を設けずまとめて禁じる。場所を変えたい場合は現チャレンジを終わらせてから新しく作る |
| 失敗回数の算出 | DB に保存するのは成功したチェックインだけ。失敗回数は画面を表示するたびに「期限を過ぎた日数 − チェックイン成功日数」で計算する。失敗を DB に持つと、誰も操作しなくても失敗を記録する仕組み（毎晩決まった時刻に自動実行される処理）が必要になるため。**両辺で数える範囲を揃える**（成功日数に、まだ期限が来ていない当日のチェックインを含めると過去の失敗を1回打ち消してしまう） |
| システム利用料 | `SYSTEM_FEE_PERCENT`。デポジット額に対する率で取る運営収益。返金対象外。**円未満は切り捨てる**（デポジット額を 500円の倍数に限っていないため端数が出る。切り上げるとユーザーの不利になる） |
| タイムゾーン | `TIME_ZONE`。サーバー・DB とも JST 固定。変換ロジックは持たない |
| 同時チャレンジ数 | `MAX_ACTIVE_CHALLENGES`。1ユーザーにつき1件だけ。並行して走らせると逃げ道と管理の複雑さが増えるため |
| 途中離脱・中断 | 中断・一時停止・中途解約はいずれも不可。やめたい場合は放置するしかなく、失敗が積み上がって早期失敗で自然終了する。逃げ道を作らないという本アプリの前提と一貫させるため。「チャレンジを中止」ボタンは作らない |
| 位置情報の偽装 | v1 では既知のリスクとして許容し、検知・ブロックのコードは書かない。実金銭が動かないため偽装しても損得が発生しない。v2 で実決済を入れる際に再検討する |
| 利用規約・プライバシーポリシー | `/terms` と `/privacy` に静的ページとして置く。位置情報という個人情報を継続的に取得する以上、どちらも欠けたまま公開しない。同意はログインをもって得たものとみなす（下記） |


**しきい値の正は [lib/config.ts](lib/config.ts)。** 上表は数値を持たず、識別子と「なぜその値か」だけを残す。値を変えるときは `lib/config.ts` を直し、必要なら上表の理由も同じコミットで直す。

## 利用規約・プライバシーポリシー

`/terms`（利用規約）と `/privacy`（プライバシーポリシー）を静的ページとして置く。認証不要・タブバーなしで、ログイン画面とマイページから開ける。初回の位置情報取得の前に出す説明にもプライバシーポリシーへのリンクを置く（[docs/screens.md](docs/screens.md)）。

**同意はログインをもって得たものとみなす。** ログイン画面に「ログインすると利用規約とプライバシーポリシーに同意したものとみなします」の一文と両ページへのリンクを置き、チェックボックスや同意日時の保存は持たない。v1 は実金銭が動かず、同意の記録を後から突き合わせる場面が存在しないため。**v2 で実決済を入れる際は同意の取得と記録をやり直す。**

### プライバシーポリシーに書く項目

位置情報の扱いは本アプリ固有なので、雛形の丸写しでは足りない。最低限これらを明記する。

- **取得する情報**: Google アカウントのプロフィール（Auth.js 経由）、目標地点の緯度・経度と名前、チェックインの記録
- **取得の方法と目的**: ブラウザの Geolocation API で取得し、目標地点との距離判定にのみ使う。常時追跡・バックグラウンド取得・IP ジオロケーションは行わない
- **保存する範囲**: 保存するのは目標地点の座標と、成立したチェックインの日付だけ。目標地点の座標は登録時に取得した現在地をそのまま保存したもので、これとは別に、チェックインのたびに判定用に取得する現在地と `accuracy` は保存しない。ログにも座標を出さない
- **第三者提供**: しない。広告・解析の目的で位置情報を渡さない
- **削除の方法**: 保存した目標地点はマイページから削除できる。アカウントごと削除したい場合の窓口も書く
- **問い合わせ先**

### 利用規約に書く項目

- **v1 では実際の金銭のやり取りが発生しない**こと。デポジット額・ペナルティ額・返金額はいずれも表示上の計算にすぎない
- 中断・一時停止・中途解約ができないこと、早期失敗時にペナルティ単価未満の端数を返さないこと（→ 仕様表「途中離脱・中断」「早期失敗」）
- 位置情報の偽装などの不正な手段でチェックインを行わないこと。v1 では検知しないが、禁止事項としては書いておく
- 判定は本アプリの計測結果に基づくこと。電波状況や端末設定による測位の失敗について責任を負わないこと
- 規約の変更方法

**文面は法務レビューを受けていない。** 実金銭が動かない v1 の前提で書く。v2 で決済を入れるときは下記の法務確認とセットで見直す。

## v1 のスコープ外

- Stripe 等による実決済・返金（方針は下記のとおり先に決めてある）
- グループ機能・他ユーザーとの共有
- プッシュ通知・アラーム音などのリマインド機能
- 複数チャレンジの同時進行

### 将来の Stripe 連携方針（決定済み・実装は v2 以降）

- **決済時**: 「デポジット額 ＋ システム利用料」を合算して決済する
- **返金時**: 期間満了まで継続した場合は「デポジット額 −（失敗回数 × ペナルティ単価）」を返金する。早期失敗の場合は 0円。システム利用料は返金しない
- 日本の Stripe 決済手数料は概ね 3.6% 前後のため、現行のシステム利用料率では完全には相殺できない。差分は許容する

#### v2 着手の前提条件（法務確認）

法務の問題がある可能性があるのでV2実装前に一度決済周りの法律を調べる。

決済を入れると特定商取引法に基づく表記が別途必要になり、利用規約・プライバシーポリシーも書き直しになる（返金条件、同意の取得と記録、決済事業者への情報提供）。法律の確認と同じタイミングで見直す。

## ドメイン用語

コードの識別子はこの語彙に揃える。

- **チャレンジ (challenge)**: 目標地点＋期限時刻＋期間＋金額のセット
- **目標地点 (target location)**: 緯度・経度。チャレンジに属し、作成時に決めて以後は変えない。チャレンジごとに違う場所を指定できる
- **保存した目標地点 (saved location)**: 現地で登録した地点の台帳。アカウントに紐づき、チャレンジ作成時の選択肢になる。チャレンジ側は選んだ時点の名前と座標を写して持つため、台帳から削除しても進行中・終了済みのチャレンジには影響しない
- **チェックイン (check-in)**: 距離判定と時刻判定を両方クリアする行為。1日1回
- **失敗 (failure)**: チェックインできなかった日
- **デポジット (deposit)**: ユーザーが入力する金額。取りうる範囲は `lib/config.ts`
- **位置精度 (accuracy)**: Geolocation が返す誤差半径。閾値を超える座標は採用しない
- **ペナルティ単価**: 1失敗あたりの減額
- **早期失敗**: 残高がペナルティ単価を下回り、チャレンジが即時終了した状態
- **システム利用料**: デポジット額に対する一定率の運営収益

## 技術スタック

- **Bun** — `npm` / `npx` / `yarn` は使わない（`npx` の代わりに `bunx`）
- **Next.js 16 (App Router) + React 19 + TypeScript** — `src/` は使わず `app/` はリポジトリ直下。学習データの Next.js とは API・規約・ファイル構成が異なる場合があるため、コードを書く前に `node_modules/next/dist/docs/` の該当ガイドを読み、非推奨の記載に従う
- **CSS Modules + SCSS** — CSS-in-JS やユーティリティクラス方式は使わない
- **MySQL + Prisma** — スキーマ・マイグレーション・クエリはすべて Prisma 経由。生 SQL は書かない
- **Auth.js (next-auth)** — ソーシャルログイン。自前のパスワード管理はしない
- **Geolocation API** — 位置情報はブラウザから取得する（IP ジオロケーションは使わない）。目標地点の登録も現地で取得した座標をそのまま記録し、地図でピンを指定する UI は導入しない。保存済みの目標地点を選ぶ場合は登録時に検証を通った座標をそのまま使い、精度チェックは行わない
- **Vitest** — 単体テスト
- **（v2 以降）Stripe** — v1 では API を一切呼ばない

未導入のものは使う段になってから `bun add` する。先回りして依存を追加しない。現状は Tailwind CSS v4 と `package-lock.json` が残っており、実装着手時に Tailwind 撤去・SCSS 移行・`bun.lock` 化を行う。

## コマンド

```bash
bun install                                # 依存インストール
bun run dev                                # 開発サーバー (http://localhost:3000)
bun run build                              # 本番ビルド。型エラーもここで出る
bun run lint                               # ESLint
bun add <パッケージ名>                      # 依存追加（開発用は -d）
bunx vitest                                # テスト
bunx prisma migrate dev --name <変更内容>   # マイグレーション作成・適用
bunx prisma generate                       # Prisma Client の型を再生成
bunx prisma studio                         # DB を GUI で確認
```

**変更後は必ず `bun run lint` と `bun run build` を通す。**

DB（MySQL / Prisma）と認証（Auth.js）のセットアップ手順は、実装に着手した時点でここに追記する。

## ディレクトリ構成（実装時にこの形に育てる）

- `app/` — ルーティング・画面・Route Handler
- `components/` — 再利用する UI コンポーネント
- `lib/` — ドメインロジック。距離判定・期限判定・金額計算は DB やリクエストに依存しない純粋関数として必ずここに置く
- `lib/config.ts` — しきい値・定数の集約先
- `prisma/schema.prisma` — DB スキーマ
- `styles/` — SCSS 変数（[docs/style.md](docs/style.md)）
- `docs/` — 開発ドキュメント

## コーディングルール

- **しきい値をハードコードしない。** 判定半径・位置精度の上限・デポジット額の上下限・チェックイン受付開始時刻・システム利用料率などの数値はすべて `lib/config.ts` に集約する
- **金額計算・距離計算をコンポーネントに書かない。** `lib/` の純粋関数として実装し、コンポーネントからは呼ぶだけにする
- **Server Components をデフォルトにする。** `"use client"` はブラウザ API が必要な末端のコンポーネントに限定する
- **Stripe の決済 API を呼ぶコードを書かない。** v1 のスコープ外。金額計算までに留める
- 金額は整数（円）で扱う。浮動小数点で金額計算をしない
- 位置情報は個人情報。必要以上に保存せず、ログに座標を出力しない。保存した目標地点はユーザーが自分で削除できるようにする。**プライバシーポリシーに書いた範囲を超えて保存・利用しない**（範囲を広げるならポリシーを同じコミットで直す）
- TypeScript は `strict: true`。`any` は使わず、必要なら `unknown` を経由する
- import は `@/` エイリアスを使う（`../../` を連ねない）
- UI 文言・コメント・コミットメッセージ・PR は日本語。識別子は英語

## 触ってはいけないファイル

- `bun.lock` — 手編集せず `bun` コマンド経由で更新する（コミット対象。削除しない）
- `prisma/migrations/` の**適用済み**ファイル — 書き換えず、新しいマイグレーションを追加して直す
- `node_modules/`、`.next/`、`next-env.d.ts` — 自動生成物
- `.env*` — 秘密情報。[docs/env.md](docs/env.md) を参照
