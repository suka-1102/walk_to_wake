# 実装ロードマップ

**進捗の正はこのファイル。** 新しいセッションはまず「現在地」を見れば続きから作業できる。

仕様は [AGENTS.md](../AGENTS.md)、ブランチ運用は [workflow.md](workflow.md)。ここは順番と進捗だけを扱う。

## 現在地

**最終更新: 2026-08-30**

- Phase 0 / Phase 0.5 完了（bun 移行、Tailwind 撤去、SCSS 導入、Vitest 導入済み。位置精度は実測済みで 50m ガード据え置き）
- 画面構成を確定（[screens.md](screens.md)）
- Phase 1 完了（距離・受付時間・期限超過日数・失敗回数と残高・システム利用料と返金額をすべて `lib/` の純粋関数として実装、テスト付き）
- 2.1 完了（`prisma`・`@prisma/client` を導入。`prisma/schema.prisma` は MySQL データソースのみでモデル未定義。`.env.example` を追加）
- 次にやること: **2.2**
- **2.2 の前に「保留中の判断」①を決める**（スキーマが変わる）

## ルール

- チェックリストの1行が1コミット。**行の文言をそのままコミットメッセージにする**
- コミットしたら `- [ ]` を `- [x]` にし、同じコミットに含める
- フェーズを終えたら「現在地」を書き換える
- 決めたことは「決定ログ」に一行残す。仕様に関わるなら AGENTS.md も同じ PR で直す
- 計画から外れたら計画のほうを直す
- 各コミットは単体で `bun run build` が通ること（Phase 0 のみ例外）
- フェーズごとに `develop` から `feature/*` を切る
- 1項目ごとの確認、push、マージの手順は [workflow.md](workflow.md)

---

## Phase 0 — 土台の入れ替え `feature/setup`

- [x] **0.1** npm から bun へ移行
- [x] **0.2** Tailwind CSS を撤去
- [x] **0.3** SCSS を導入しスタイルの土台を作成
- [x] **0.4** Vitest を導入

撤去（0.2）と導入（0.3）は分ける。1コミットにすると壊れたときの切り分けができない。

完了の目安: `bun run build` が通り、`bunx vitest` がテスト0件で正常終了する。

## Phase 0.5 — 位置情報の実地確認 `feature/geolocation-spike`

- [x] **0.5.1** 位置情報の精度確認用ページを追加

現在地の緯度経度と `accuracy` を出すだけのページを作り、端末を持って目標地点にしたい場所へ行き、実際に何メートルで返るかを見る。**このブランチは `develop` へマージしない。**

精度ガード 50m が現実的でないと分かったら判定半径ごと決め直すことになる。DB スキーマを書く前に知りたいので早めにやる。見直す場合は AGENTS.md と `lib/config.ts` を直すコミットだけを `develop` に入れ、決定ログに実測値を残す。

**実測結果（2026-08-20 / iOS）: 最悪値も 50m 以内。しきい値は見直さない。** ページは `feature/geolocation-spike` に残す（`develop` へはマージしない）。再計測が必要になったらそのブランチに戻る。

**Android は未計測。** Android Chrome は初回に粗い座標を返してから収束する挙動があるため、Phase 4 で実機確認する。そのため**ブランチは Phase 4 の Android 実測が済むまで残し、済んだ時点で削除する**。

## Phase 1 — 設定値と純粋関数 `feature/domain-logic`

- [x] **1.1** しきい値を lib/config.ts に集約
- [x] **1.2** 距離計算と判定半径の内外判定を追加
- [x] **1.3** チェックイン受付時間の判定を追加
- [x] **1.4** 期限を過ぎた日数の算出を追加
- [x] **1.5** 失敗回数と残高の算出を追加
- [x] **1.6** システム利用料と返金額の計算を追加

1.1 は同じコミットで AGENTS.md の仕様表から数値を消し、「なぜその値か」だけ残す。

1.2 以降はすべてテストとセット。境界値（残高 500円/499円、期限ちょうど/1秒後、4:00 の前後、開始日当日、終了日翌日）を必ず入れる。

**時刻は引数で受け取る。** 関数内で `new Date()` を呼ぶと結果が実行時刻に左右され、テストが書けない。

## Phase 2 — DB `feature/db`

- [x] **2.1** Prisma を導入
- [ ] **2.2** チャレンジ・チェックイン・保存した目標地点のスキーマを追加
- [ ] **2.3** Prisma Client の共有インスタンスを追加

`CheckIn` の `(challengeId, date)` に一意制約を張り、「1日1回」を DB 側で担保する。

**`Challenge` は目標地点の名前と座標を写して持つ。** `SavedLocation` を参照するだけにすると、台帳から削除されたときに進行中・終了済みのチャレンジが目標地点を失う。写しておけば台帳は選択肢の置き場に徹せられ、削除を自由に許せる。

**アカウント削除を見越して、`User` に紐づくものは削除が連鎖するようにしておく。** 保存した目標地点・チャレンジ・チェックインが対象。6.6 でアカウント削除を作る段になってからスキーマを直すと、適用済みのマイグレーションを追加で重ねることになる。表示名を持つカラムも 2.2 で一緒に足す。

**2.2 の前に保留中の判断①を決める。** スキーマが変わる。

## Phase 3 — 認証 `feature/auth`

- [ ] **3.1** Auth.js による Google ログインを追加
- [ ] **3.2** 認証情報を DB に保存するよう設定
- [ ] **3.3** ログイン画面とセッションガードを追加
- [ ] **3.4** 利用規約とプライバシーポリシーのページを追加

これ以降の画面はすべて「誰の」チャレンジかを必要とする。後から入れると全画面を直すことになる。

**3.4 は 3.3 と同じフェーズに置く。** ログイン画面に「ログインすると同意したものとみなします」の文言とリンクを出す以上、リンク先が存在しない状態でフェーズを終えられない。書く項目は [AGENTS.md](../AGENTS.md) の「利用規約・プライバシーポリシー」が正。middleware のリダイレクト対象から `/terms` と `/privacy` を外すのを忘れない（ログイン前に読めなければ同意が成り立たない）。フッターからのリンクは 6.3 で足す。

## Phase 4 — チャレンジ作成 `feature/challenge-create`

- [ ] **4.1** 位置情報を取得するフックを追加
- [ ] **4.2** 位置精度が不足する場合の再取得 UI を追加
- [ ] **4.3** 目標地点の登録処理を追加
- [ ] **4.4** 登録した目標地点を保存し、一覧から選べるようにする
- [ ] **4.5** チャレンジ入力値の検証を追加
- [ ] **4.6** チャレンジの作成処理を追加
- [ ] **4.7** チャレンジ作成画面を追加

**目標地点の登録は独立した画面ではない。** [screens.md](screens.md) では `/challenges/new` が目標地点・期限時刻・期間・デポジット額をまとめて受け取るため、目標地点だけの画面は存在しない。フェーズを分けると 4.7 まで載せる先がない部品が積み上がるので、作成画面までを1フェーズで作る。

4.1 は「取得できるか」、4.2 は「採用してよいか」で責務が違うので分ける。4.2 は Phase 5 で再利用する。

4.2 では**1回の取得で弾かず、数秒 `watchPosition` して収束を待ってから判定する**。初回のコールバックは Wi-Fi・基地局ベースの粗い座標が返ることがあり、そこで弾くと無用な失敗になる。

**iOS の「正確な位置情報」が OFF だと accuracy が桁違いに悪化し、必ずガードに弾かれる。** 精度不足の文言には端末設定の確認を促す一文を含める。

4.4 で扱うのは保存と選択まで。保存した目標地点の削除はマイページに置くため 6.6 で入れる。

4.1〜4.4 は位置情報。前半が終わった時点で一度実機確認を挟んでから後半に入る。

完了の目安: DevTools の Sensors パネルで座標と精度を偽装し、通る場合と弾かれる場合の両方を確認した。

## Phase 5 — チェックイン `feature/check-in`

- [ ] **5.1** チェックインの成立判定を追加
- [ ] **5.2** チェックインの登録処理を追加
- [ ] **5.3** 1日1回の制限を追加
- [ ] **5.4** チェックイン画面を追加

**判定はサーバー側で行う。** クライアントは座標と `accuracy` を送るだけ。クライアント時刻を信じると、端末の時計をずらすだけで期限を回避できる。

5.1 で不成立の理由を型として返しておくと、5.4 は分岐を書くだけで済む。

## Phase 6 — ホームと共通レイアウト `feature/dashboard`

- [ ] **6.1** チャレンジの集計処理を追加
- [ ] **6.2** ホーム画面を追加
- [ ] **6.3** ヘッダー・ハンバーガーメニュー・フッターを追加
- [ ] **6.4** チャレンジ詳細の画面を追加
- [ ] **6.5** チャレンジ詳細に成功・失敗した日の一覧を追加
- [ ] **6.6** 履歴とマイページ（保存した目標地点の一覧と削除、表示名の変更、アカウント削除、ログアウト）を追加

画面構成とパスは [screens.md](screens.md) が正。座標を扱わないので Server Component で完結する。

6.3 のヘッダーとフッターは全ページが乗る共有レイアウト。ハンバーガーはヘッダー右端に置き、メニューは4項目（ホーム / チェックイン / 履歴 / マイページ）。ログイン後だけハンバーガーを出す。フッターは規約・ポリシーへのリンクだけを持ち、ログイン前後を問わず出す。6.2 まではヘッダーなしで作り、画面が2つ以上そろってから入れる。**開閉状態を持つのはハンバーガーメニューだけなので、`"use client"` はそのコンポーネントに閉じる。**

6.4 の詳細は進行中と終了済みの共用。6.5 の記録もその中に置き、記録だけの画面は作らない。6.2 のホームは進行中のチャレンジ1件を出すだけで、詳細への導線を持たせる。

**6.6 のアカウント削除は取り返しがつかない。** 確認ダイアログを挟み、目標地点・チャレンジ・チェックインの記録がすべて消えることと、進行中でもデポジットが戻らないことを削除前に出す。削除後はログアウトして `/login` へ戻す。

## Phase 7 — 仕上げ `feature/polish`

- [ ] **7.1** 空の状態の画面を追加
- [ ] **7.2** エラー画面を追加
- [ ] **7.3** スタイルを整理しレスポンシブ対応
- [ ] **7.4** UI 文言を統一

---

## 保留中の判断

決まったら決定ログへ移す。

**① 終了状態を DB に持つか（2.2 まで）**
失敗回数は動的算出なので DB に終了フラグが存在せず、「1ユーザー1チャレンジ」を一意制約で表現できない。`Challenge.endedAt`（null ならアクティブ）を持たせ、アクセス時に終了条件を満たしていたら一度だけ書き込む方式が素直。

**② チャレンジ期間の上限（4.5 まで）**
デポジット 30,000円なら 60回失敗できるため、期間が 60日を超えると満了より早期失敗が先に来る組み合わせが生まれる。上限を置くか、自然に任せるか。

**③ 開始日は当日からか、翌日以降か（4.5 まで）**
当日開始を許す場合、期限時刻を過ぎた後に作成されたときの初日の扱いを決める必要がある。

**④ 保存した目標地点をアカウント設定の下にまとめるか（6.6 まで）**
マイページに「保存した目標地点」と「アカウント設定」を別々の区分として並べている。設定の一部として畳んだほうが収まりがよいかもしれないが、目標地点の削除はプライバシーポリシーで案内する導線でもあるため、埋もれさせたくない。実際に画面を作ってから決める。

デポジット額を 500円の倍数に限るかは未決だが、限らなくても仕様は成立する（端数は早期失敗時に没収される）。決めずに進めてよい。

## 決定ログ

- **2026-08-16** 未確定事項5件（GPS精度の扱い / 位置情報の偽装 / デポジット額の上下限 / 途中離脱 / 法務確認）を確定し AGENTS.md に反映
- **2026-08-20** 画面構成を確定し screens.md を追加。スマホ縦画面を基準とし、ログイン後は4つの下部タブ（ホーム / チェックイン / 記録 / マイページ）で切り替える。進行中は `/challenges/current`、終了済みは `/challenges/[id]` に分ける
- **2026-08-20** チャレンジ作成への導線はホームと結果画面の2箇所だけに置く。チェックインタブと記録タブは空の状態で文言のみを出す。記録タブが扱うのは進行中のチャレンジだけとし、終了分は履歴で見る
- **2026-08-20** iOS 実機で位置精度を実測。最悪値も 50m 以内だったため精度ガード 50m・判定半径 100m を据え置き（Phase 0.5 完了）
- **2026-08-21** `feature/geolocation-spike` は Phase 4 の Android 実測で再利用するため、その時点まで削除しない。`develop` へ入れるのは実測結果の記録だけとする
- **2026-08-22** ログイン後のタブ4つをすべてトップレベルのパスに揃える。ホームを `/challenges/current` から `/` へ、マイページを `/account` から `/mypage` へ移し、ログイン画面を `/login` に切り出す（Auth.js の `pages.signIn`）。タブバー左端のホームだけがネストされたパスになっていたため
- **2026-08-22** チャレンジ開始後は目標地点も変更不可とし、変更回数の仕組みを廃止する。目標地点と期限時刻はどちらも後から条件を緩めて失敗を逃れる同じ形のチートなのに、前者だけ 3 回許していて非対称だった。場所はチャレンジごとに自由に選べ、途中で変えたければ現チャレンジを終わらせる（残高を失う）しかない。`MAX_LOCATION_CHANGE_COUNT` と変更拒否のテスト項目を削除し、マイページから残り回数の表示を外した
- **2026-08-22** 登録した目標地点をアカウントに保存し、次のチャレンジ作成時に一覧から選べるようにする。毎回同じ場所へ足を運んで登録し直すのは手間なだけで抑止力にならない。選択肢に載るのは過去に一度は現地で取得した座標だけなので、地図でピンを置く方式と違って新しい抜け道は生まれない。座標だけでは判別できないため保存時に名前を入力させ、削除はマイページに置く（位置情報を必要以上に持ち続けないため）
- **2026-08-22** 目標地点の登録フェーズをチャレンジ作成フェーズに統合し、以降のフェーズを1つずつ繰り上げる。screens.md では目標地点の登録が `/challenges/new` の一部で独立した画面を持たないのに、ロードマップだけ別フェーズ・別ブランチに分かれていて対応が取れていなかった
- **2026-08-22** 利用規約（`/terms`）とプライバシーポリシー（`/privacy`）を静的ページとして持つことを仕様に追加。位置情報という個人情報を継続的に取得する以上、どちらも欠けたまま公開できない。同意はログインをもって得たものとみなし、チェックボックスと同意日時の保存は持たない（v1 は実金銭が動かず、同意の記録を突き合わせる場面がないため。v2 の決済導入時にやり直す）。ページ追加は Phase 3 の 3.4、マイページからのリンクは 6.6
- **2026-08-22** 記録の独立した画面（`/records`）とタブを廃止し、日ごとの成功・失敗の記録をチャレンジ詳細（`/challenges/[id]`）に含める。タブは3つ（ホーム / チェックイン / マイページ）。記録はホームが出す失敗回数・残高の内訳にすぎず、集計と内訳を別タブに分けると、進行中のチャレンジがないときに文言しか出せないタブが1枠残るため。ホームは従来どおり進行中のチャレンジ1件（なければ作成の導線）を出し、そこから詳細を開く。詳細は進行中と終了済みで共用し、旧「チャレンジ結果」の画面がそのまま進行中も扱う。`/challenges` は複数形のまま（通算では複数持つため）
- **2026-08-22** 履歴をマイページの中から出して独立したタブにする（タブは4つ: ホーム / チェックイン / 履歴 / マイページ）。終了したチャレンジは使うほど積み上がり、記録を見返す先になるのに、設定と同じ引き出しに入れると入口が分かりにくい。パスは `/challenges` のまま（複数形を保つ理由は screens.md）
- **2026-08-22** `/challenges/new` でもタブバーを出す。入力途中に他タブへ移動されると入力内容が失われるため隠していたが、作成画面だけタブバーが消える不統一のほうが負担が大きい。ログイン後にタブバーを出さないのは `/login`・`/terms`・`/privacy` だけとする
- **2026-08-22** `/challenges/new` と `/challenges/[id]` ではどのタブも選択中にしない。どちらもホームと履歴の両方から開くため、片方を選択中に見せると入口を偽ることになる。タブバーは移動手段としてだけ出す
- **2026-08-26** システム利用料の円未満は切り捨てる。デポジット額を 500円の倍数に限っていないため料率をかけると端数が出る。切り上げ・四捨五入はユーザーの不利になる場合があるので取らない
- **2026-08-26** 下部タブバーを廃止し、全ページ共通のヘッダー（ハンバーガーメニュー）とフッターに切り替える。移動手段はハンバーガーメニュー1つに一本化し、タブバーとの併存はしない。同じ行き先への入口が2つあると、どちらを押しても同じという説明が必要になるため。ハンバーガーはヘッダー右端に置く（スマホを片手で持ったとき親指が届くのが右下側で、左上は最も遠い）。2026-08-20 以降のタブ前提の決定（タブ4つ / 履歴を独立タブに / `/challenges/new` でもタブバーを出す / どのタブも選択中にしない）は、行き先と選択中の扱いを引き継いだうえでメニューの決定として読み替える
- **2026-08-26** フッターを全ページに置き、利用規約・プライバシーポリシーへの入口をそこに一本化する。以前は「スマホ縦画面でタブバーと二重になる」ためフッターを持たない方針だったが、タブバーを廃止したので理由が消えた。マイページからのリンクは外す（全ページにフッターが出る以上、同じ行き先への入口が二重になるため）。ログイン画面だけは同意の文言とセットで見せる必要があるのでボタン下にもリンクを残す。**著作権表示は置かない**（個人が自分1人のために作るアプリで権利を主張する相手がおらず、規約への導線という本来の役割がぼやけるため）
- **2026-08-26** マイページにアカウント設定（表示名の変更・アカウント削除）を置く。表示名は Google プロフィールを初期値としてアプリ内だけで上書きでき、Google 側は変更しない。アカウント削除は確認を挟んで目標地点・チャレンジ・チェックインの記録をすべて消す。プライバシーポリシーに削除の方法を書く以上、問い合わせ窓口だけで済ませずユーザー自身が消せる導線を持たせる。どちらも `User` のスキーマに影響するため 2.2 で一緒に入れる
- **2026-08-26** 失敗回数の上限を「デポジット額が吸収できるペナルティ回数」とする。失敗回数は「期限を過ぎた日数 − チェックイン成功日数」で毎回算出するため、早期失敗で終了した後も日数だけが増え続け、終わったはずのチャレンジの失敗回数が伸びてしまう。上限で頭打ちにすれば、終了時点の失敗回数・残高・返金額がそのまま固定される
